class PageViewModel {
  const PageViewModel({
    required this.imageWidth,
    required this.imageHeight,
    required this.imageWidthScaled,
  });

  final double imageWidth;
  final double imageHeight;
  final double imageWidthScaled;
}
